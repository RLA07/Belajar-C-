#include <iostream>
using namespace std;
int main() {
    int m = 166;
    cout << "(m>=0 && m <=150) -> " << (m>=0 && m<=150) << endl;
    cout << "(m>=0 || m <=150) -> " << (m>=0 || m<=150) << endl;
}